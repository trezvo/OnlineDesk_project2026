syntax = "proto3";

package desk;

service Greater{
    rpc Echo(EchoRequest) returns (EchoResponse);
}

message EchoRequest{
    string text = 1;
}

message EchoResponse{
    bool success = 1;
    string text = 2;
}